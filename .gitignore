# Secrets — never commit real account tokens or the service API key
accounts.json
.env

# Runtime state — generated at runtime, not source
state.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# OS/editor cruft
.DS_Store
